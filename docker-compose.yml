services:
  auth-service:
    build: ./auth-service
    container_name: auth-service
    ports:
      - "8080:8080"
    environment:
      # Configuración para H2 en memoria
      - SPRING_DATASOURCE_URL=jdbc:h2:mem:donaton_db
      - SPRING_DATASOURCE_DRIVER_CLASS_NAME=org.h2.Driver
      - SPRING_DATASOURCE_USERNAME=sa
      - SPRING_DATASOURCE_PASSWORD=
      - SPRING_H2_CONSOLE_ENABLED=true

  frontend:
    build: ./frontend
    container_name: donaton-frontend
    ports:
      - "80:80"
    depends_on:
      - auth-service

  # Futura base de datos (Comentar/Descomentar según se necesite)
  # db:
  #   image: postgres:18
  #   container_name: donaton-db
  #   environment:
  #     POSTGRES_DB: donaton_db
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
  #   ports:
  #     - "5432:5432"